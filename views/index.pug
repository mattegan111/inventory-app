extends layout

block content
  h1= title
  p Welcome to the #[em Inventory App]

  if error
    p Error getting dynamic content.
  else
    p The inventory has the following records:

    ul
      li #[strong Product Types:] !{data.product_type_count}
      li #[strong Product Units in Total:] !{data.product_units_count}
      li #[strong Manufacturers:] !{data.manufacturer_count}
      li #[strong Categories:] !{data.category_count}